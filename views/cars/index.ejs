<%- include('../partials/header') %>

                <h1 class='title'>Car Reviews</h1>
                <p class='title-description'>______________________________</p>
    <div class='carTable'>
        <table class='content'>
            <thread>
                <tr>
                    <th>Make</th>
                    <th>Model</th>
                    <th>Year</th>
                    <th>Date Posted</th>
                    <th>Author</th>
                </tr>
            </thread>
            <tbody>
                <% cars.forEach(function(c){ %>
                    <tr class='key'>
                        <td class='make'><%= c.make %></td>
                        <td class='model'><%= c.model %></td>
                        <td class='year'><%= c.year %></td>
                        <td class='posted'><%= c.posted.toLocaleDateString() %></td>
                        <td class='author'><%= c.author %> </td>
                        <td><a class='allButtons' href="/cars/<%= c.id %>">DETAILS</a></td>
                    </tr>
                    <% }) %> 
            </tbody>
        </table>
    </div>

    <%- include('../partials/footer') %>